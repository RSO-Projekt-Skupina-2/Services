apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: microhub-posts
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: microhub-posts
  namespaceSelector:
    matchNames:
      - ingress
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
